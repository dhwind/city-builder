type ComponentProps = {
  color: string;
  floors: number;
  id: number;
  name: string;
};

const BuildingItem: React.FC<ComponentProps> = ({}) => {
  return <div />;
};

export default BuildingItem;
